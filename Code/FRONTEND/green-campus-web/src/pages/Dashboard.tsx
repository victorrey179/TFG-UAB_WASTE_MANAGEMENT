import React from "react";
import Layout from "../components/Layout";

const Dashboard: React.FC = () => {
  return (
    <Layout>
      <div>
        <h1>Dashboard</h1>
        {/* Agrega tus widgets o contenido aquí */}
      </div>
    </Layout>
  );
};

export default Dashboard;
